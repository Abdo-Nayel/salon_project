# Deploy Guide

Full deployment documentation: **[deploy/DEPLOY.md](deploy/DEPLOY.md)**

Quick start for a new client (e.g. **sweb**):

```bash
bash deploy/new-client.sh sweb Sweb 'StrongDbPassword123'
```

Then follow the steps in `deploy/DEPLOY.md`.
